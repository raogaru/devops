# create ddl scripts
tabgen scripts/pxses/tabgen.yaml

# generate individual scripts for testing parallel (px) session activity
sh gen.sh

files will be generated in sql directory. which has pg_sleep to sleep between 0 and 10 seconds. helpful to monitor parallelism 

# copy content of x.yaml  into jobrun.yaml group:pxses_activity  and change parallel: to 5

# tun job

jobrun.py scripts/pxses/jobrun.yaml


DROP TABLE IF EXISTS sample.customers ;
CREATE TABLE sample.customers (
    customer_id   bigserial PRIMARY KEY,
    country       text NOT NULL,
    segment       text NOT NULL,
    created_at    timestamptz NOT NULL
);


# Use official MySQL image as base
FROM mysql:8.0

# Set environment variables (default credentials)
ENV MYSQL_ROOT_PASSWORD=root
ENV MYSQL_DATABASE=mysql
ENV MYSQL_USER=mysql
ENV MYSQL_PASSWORD=mysql

# Expose MySQL port
EXPOSE 3306


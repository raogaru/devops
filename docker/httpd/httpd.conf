# ============================================================
# Basic Apache HTTP Server Configuration
# ============================================================

ServerRoot "/usr/local/apache2"
Listen 80

# Load core modules
LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule dir_module modules/mod_dir.so
LoadModule mime_module modules/mod_mime.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule alias_module modules/mod_alias.so
LoadModule rewrite_module modules/mod_rewrite.so

# Server identity
ServerAdmin webmaster@localhost
ServerName localhost

# Default document root
DocumentRoot "/usr/local/apache2/htdocs"

<Directory "/usr/local/apache2/htdocs">
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
</Directory>

# ============================================================
# Logging
# ============================================================
ErrorLog "/usr/local/apache2/logs/error.log"
LogLevel warn

<IfModule log_config_module>
    LogFormat "%h %l %u %t \"%r\" %>s %b" common
    CustomLog "/usr/local/apache2/logs/access.log" common
</IfModule>

# ============================================================
# Directory index and MIME types
# ============================================================
<IfModule dir_module>
    DirectoryIndex index.html
</IfModule>

TypesConfig conf/mime.types
DefaultType text/plain

# ============================================================
# VirtualHost example (optional)
# ============================================================
# <VirtualHost *:80>
#     ServerName example.local
#     DocumentRoot "/usr/local/apache2/htdocs/example"
#     ErrorLog "/usr/local/apache2/logs/example-error.log"
#     CustomLog "/usr/local/apache2/logs/example-access.log" common
# </VirtualHost>

# ============================================================
# Include extra configuration if needed
# ============================================================
# IncludeOptional conf/extra/httpd-vhosts.conf
# IncludeOptional conf/extra/httpd-default.conf

